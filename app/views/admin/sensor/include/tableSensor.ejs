<div class="box box-primary">
  <div class="box-header">
    <h3 class="box-title"></h3>
    <a href="/admin/sensor/create/<%= equipo._id %>" class="btn btn-success"><i class="fa fa-plus"></i></a>
    <div class="box-tools">
      <div class="input-group input-group-sm" style="width: 150px;">
        <input type="text" name="table_search" class="form-control pull-right" placeholder="Search">

        <div class="input-group-btn">
          <button type="submit" class="btn btn-default"><i class="fa fa-search"></i></button>
        </div>
      </div>
    </div>
  </div>
  <!-- /.box-header -->
  <div class="box-body table-responsive no-padding">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Tipo</th>
          <th>Unidad</th>
          <th>Marca</th>
          <th>Modelo</th>
          <th>Serie</th>
          <th>Ubicacion</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% sensores.forEach(function(sensor){ %>
          <tr>
            <td><%= sensor.tipo %></td>
            <td><%= sensor.unidad %></td>
            <td><%= sensor.marca %></td>
            <td><%= sensor.modelo %></td>
            <td><%= sensor.serie %></td>
            <td><%= sensor.ubicacion %></td>
            <td>
              <% if (sensor.estado) { %>
                <form action="/admin/sensor/update/<%= sensor._id %>" method="POST" style="display: inline;">
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                  <input type="hidden" name="estado" value="false">
                  <input type="hidden" name="idEquipment" value="<%= equipo._id %>">
                  <button type="submit" class="btn btn-danger">Desactivar</button>
                </form>
              <% } else { %>
                <form action="/admin/sensor/update/<%= sensor._id %>" method="POST" style="display: inline;">
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                  <input type="hidden" name="estado" value="true">
                  <input type="hidden" name="idEquipment" value="<%= equipo._id %>">
                  <button type="submit" class="btn btn-primary">Activar</button>
                </form>
              <% } %>
            </td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  </div>
  <!-- /.box-body -->
  <div class="box-footer clearfix">
    <%- include('../../../layout/pagination') %>
  </div>
</div>
